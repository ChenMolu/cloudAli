server:
  port: 8001

spring:
  application:
    name: cloud-payment-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource            # 当前数据源操作类型
    driver-class-name: org.gjt.mm.mysql.Driver              # mysql驱动包 com.mysql.jdbc.Driver
    url: jdbc:mysql://8.134.113.47:3306/db2019?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: lh90816


mybatis:
  mapperLocations: classpath:mapper/*.xml
  type-aliases-package: com.rocky.springcloud.entities    # 所有Entity别名类所在包